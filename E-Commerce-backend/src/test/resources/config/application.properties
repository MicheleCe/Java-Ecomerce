# This file put in config folder is to override properties in the main application.properties without replacing the file.

spring.datasource.url=jdbc:h2:mem:test
spring.datasource.driver-class-name=org.h2.Driver
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.H2Dialect
# in order to prevent the loading of the sql data before the autoddl kicks in, with true it runs first
spring.jpa.defer-datasource-initialization=true

# SMTP configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=no.reply.ecotester@gmail.com
spring.mail.password=lvbr kohv eocu wtru
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

